AIDS (Artifical Intelligence Decider on Sex) is a
simple generate with some memory and basic AI elements
to generate random kink related words.

Simply run the IDLE and type g() or generate() to
generate a kink. You can call on the list by typing
kl.<list>. More info on the kink_lists.